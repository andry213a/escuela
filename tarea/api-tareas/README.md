# API Tareas (minimal)

Requisitos: Node.js, Git, VS Code, navegador.

Instalación:
1. `npm install`
2. `npm run dev`  (requiere nodemon) o `npm start`

La app correrá en `http://localhost:3000`. El frontend está en la raíz del servidor.
